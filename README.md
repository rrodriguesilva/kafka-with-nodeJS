# Criar arquivo .env

Dentro do arquivo **.env** colocar uma variável: **DOCKER_IP**

# Iniciar o docker

`docker-compose up -d`

# Instalar os pacotes

`yarn install`

#Iniciar o projeto

`yarn start`
